---
const { skill, icon } = Astro.props;
---
<div class="flex flex-col items-center p-4 bg-gray-100 rounded shadow-lg transform hover:scale-105 transition-transform">
  <img src={icon} alt={skill} class="w-12 h-12" />
  <p class="mt-4 text-lg font-semibold">{skill}</p>
</div>
